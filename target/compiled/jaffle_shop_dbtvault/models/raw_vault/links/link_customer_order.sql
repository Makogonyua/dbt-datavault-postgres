-- Generated by AutomateDV (formerly known as dbtvault)

    

WITH row_rank_1 AS (
  SELECT * FROM (
    SELECT rr.LINK_CUSTOMER_ORDER_PK, rr.CUSTOMER_PK, rr.ORDER_PK, rr.LOAD_DATE, rr.RECORD_SOURCE,
           ROW_NUMBER() OVER(
               PARTITION BY rr.LINK_CUSTOMER_ORDER_PK
               ORDER BY rr.LOAD_DATE
           ) AS row_number
    FROM "postgres"."dbt"."stg_link_customer_order" AS rr
    WHERE rr.LINK_CUSTOMER_ORDER_PK IS NOT NULL
    AND rr.CUSTOMER_PK IS NOT NULL
    AND rr.ORDER_PK IS NOT NULL
  ) as l
  WHERE row_number = 1
),

records_to_insert AS (
    SELECT a.LINK_CUSTOMER_ORDER_PK, a.CUSTOMER_PK, a.ORDER_PK, a.LOAD_DATE, a.RECORD_SOURCE
    FROM row_rank_1 AS a
    LEFT JOIN "postgres"."dbt"."link_customer_order" AS d
    ON a.LINK_CUSTOMER_ORDER_PK = d.LINK_CUSTOMER_ORDER_PK
    WHERE d.LINK_CUSTOMER_ORDER_PK IS NULL
)

SELECT * FROM records_to_insert